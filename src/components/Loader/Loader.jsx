import { CircularProgress } from '@material-ui/core';

import './Loader.scss';

export const Loader = () => {
    return (
        <div className="loader">
            {/* <CircularProgress /> */}
            loading...
        </div>
    );
};
